# Hamkasb.AI

AI Коллега для банков и организаций - система виртуальных AI-агентов для автоматизации бизнес-процессов.

## 🏗️ Структура проекта

Проект организован как монорепозиторий с использованием Bun workspaces:

```
hamkasb-ai/
├── apps/
│   ├── web/          # Next.js frontend
│   └── api/          # Elysia backend
├── packages/
│   ├── core/         # Shared domain logic
│   ├── ui/           # Shared UI components
│   ├── i18n/         # Internationalization
│   └── config/       # Shared configs
```

## 🚀 Быстрый старт

### Требования

- Bun >= 1.0.0
- Node.js >= 18.0.0
- Docker (для локального Postgres)
- pnpm (для Next.js проекта)

### Установка зависимостей

```bash
# Установка всех зависимостей
bun install

# Или установка pnpm для Next.js
npm install -g pnpm
cd apps/web && pnpm install
```

### Настройка окружения

1. Скопируйте `.env.example` в `.env.local`:

```bash
cp .env.example .env.local
```

2. Заполните необходимые переменные окружения в `.env.local`

### Запуск локального Postgres

```bash
# Запуск через Makefile
make postgres-start

# Создание базы данных
make postgres-create-db

# Проверка статуса
make postgres-status
```

### Запуск приложения

```bash
# Запуск всего проекта (backend + frontend)
bun run dev

# Или по отдельности:
# Backend (Elysia) на порту 3001
bun run dev:api

# Frontend (Next.js) на порту 3000
bun run dev:web
```

## 📦 Команды

### Использование Makefile (рекомендуется)

```bash
# Показать все команды
make help

# Полная настройка проекта
make setup

# Запуск разработки
make dev

# Проверка кода
make check

# Работа с БД
make db-generate  # Генерация миграций
make db-migrate   # Применение миграций
```

Подробнее см. [MAKEFILE.md](./MAKEFILE.md)

### Прямые команды

- `bun run dev` - запуск всего проекта в режиме разработки
- `bun run build` - сборка всех проектов
- `bun run lint` - проверка кода линтером
- `bun run type-check` - проверка типов TypeScript

## 🛠️ Технологический стек

### Backend
- **Runtime:** Bun.js 1.x
- **Framework:** Elysia.js
- **AI SDK:** Vercel AI SDK v6
- **Database:** PostgreSQL (Vercel Postgres в production)
- **Vector DB:** Qdrant Cloud

### Frontend
- **Framework:** Next.js 14+ (App Router)
- **Language:** TypeScript
- **Styling:** TailwindCSS
- **State Management:** React Query + Zustand
- **AI Integration:** @ai-sdk/react

## 📚 Документация

Подробная техническая спецификация находится в `info/stage-2-technical-spec.md`

## 🚢 Деплой

Проект настроен для деплоя на Vercel. Конфигурация находится в `vercel.json`.

## 📝 Лицензия

Private project

