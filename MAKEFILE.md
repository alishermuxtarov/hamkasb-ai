# Makefile - Команды для разработки

Makefile содержит все необходимые команды для работы с проектом.

## 📋 Быстрый старт

```bash
# Показать все доступные команды
make help

# Полная настройка проекта (Postgres + зависимости + БД)
make setup

# Запуск всех сервисов
make dev
```

## 🔧 Установка зависимостей

```bash
# Установить все зависимости (root, api, web)
make install

# Установить только зависимости API
make install-api

# Установить только зависимости Web
make install-web
```

## 🚀 Разработка

```bash
# Запустить все сервисы (API + Web)
make dev

# Запустить только API сервер (порт 3001)
make dev-api

# Запустить только Web сервер (порт 3000)
make dev-web
```

## 🗄️ База данных (Drizzle)

### Генерация миграций

```bash
# Генерировать миграции (аналог alembic autogenerate)
# Сравнивает текущую схему с БД и создает миграции
make db-generate
```

Эта команда:
- Анализирует схемы в `apps/api/src/lib/db/schema/`
- Сравнивает с текущим состоянием БД
- Генерирует SQL миграции в `apps/api/src/lib/db/migrations/`

### Применение миграций

```bash
# Применить миграции к БД
make db-migrate

# Или push схемы без миграций (для разработки)
make db-push
```

### Работа с БД

```bash
# Открыть Drizzle Studio (веб-интерфейс для БД)
make db-studio

# Сбросить БД (остановить, запустить, применить схемы)
make db-reset
```

## ✅ Проверка кода

```bash
# Запустить все проверки (lint + type-check)
make check

# Только линтеры
make lint

# Только проверка типов
make type-check
```

### Отдельные проверки

```bash
# Линтинг
make lint-api      # API
make lint-web      # Web

# Проверка типов
make type-check-api  # API
make type-check-web  # Web
```

## 🐘 Локальный Postgres (утилиты)

```bash
# Показать статус подключения
make postgres-status

# Подключиться через psql
make postgres-psql

# Удалить базу данных (⚠️ опасная операция)
make postgres-drop-db
```

**Примечание:** Предполагается, что Postgres уже запущен и база данных `hamkasb_ai` уже создана.

## 🏗️ Сборка

```bash
# Собрать все проекты
make build

# Собрать отдельно
make build-api
make build-web
```

## 🧹 Очистка

```bash
# Очистить артефакты сборки
make clean

# Очистить отдельно
make clean-api
make clean-web

# Полная очистка (включая node_modules)
# ⚠️ Требует переустановки зависимостей
make clean-all
```

## 🧪 Тестирование

```bash
# Запустить все тесты
make test

# Отдельно
make test-api
make test-web
```

## 📊 Утилиты

```bash
# Показать статус всех сервисов
make status

# Полная настройка проекта
make setup
```

## 🔄 Типичный workflow

### Первая настройка проекта

```bash
# 1. Установить зависимости
make install

# 2. Применить схемы БД
make db-push

# 3. Запустить разработку
make dev
```

**Примечание:** Убедитесь, что Postgres запущен и база данных `hamkasb_ai` создана.

### Ежедневная работа

# 2. Запустить сервисы
make dev

# 3. После изменений схемы - сгенерировать миграции
make db-generate

# 4. Применить миграции
make db-migrate
```

### Перед коммитом

```bash
# Проверить код
make check
```

## 📝 Примечания

- **db-generate** - аналог `alembic autogenerate` в Python/Alembic
  - Анализирует изменения в схемах
  - Генерирует SQL миграции
  - Не применяет миграции автоматически

- **db-push** - для быстрой разработки
  - Применяет схемы напрямую без миграций
  - Удобно для локальной разработки
  - Не используйте в production

- **db-migrate** - для production
  - Применяет версионированные миграции
  - Безопасно для production
  - Отслеживает историю изменений

## 🆘 Решение проблем

### Проблемы с подключением к Postgres

```bash
# Проверить статус подключения
make postgres-status

# Проверить подключение вручную
psql -h localhost -U $(USER) -d hamkasb_ai -c "SELECT version();"

# Убедитесь, что DATABASE_URL в .env.local правильный
```

### Ошибки миграций

```bash
# Сбросить БД и применить схемы заново
make db-reset
```

### Проблемы с зависимостями

```bash
# Полная очистка и переустановка
make clean-all
make install
```

